---
- name: Configurer MySQL et installer iTop 3.x en mode silencieux
  hosts: itop
  become: yes

  vars:
    mysql_root_user: root
    mysql_root_password: "root"     # Remplacer par ton mot de passe root MySQL
    itop_db_name: itop
    itop_db_user: itopuser
    itop_db_password: itoppass
    itop_install_dir: "/var/www/html/web"

  tasks:
    - name: Modifier bind-address pour autoriser toutes les connexions
      lineinfile:
        path: /etc/mysql/mysql.conf.d/mysqld.cnf
        regexp: '^bind-address\s*='
        line: 'bind-address = 0.0.0.0'
      notify: Restart MySQL

    - name: Redémarrer MySQL
      service:
        name: mysql
        state: restarted

    - name: Installer python3-full et pip
      apt:
        name:
          - python3-full
          - python3-pip
        state: present
        update_cache: yes

    - name: Installer PyMySQL via pip
      pip:
        name: pymysql
        executable: pip3

    - name: Créer base de données iTop
      community.mysql.mysql_db:
        name: "{{ itop_db_name }}"
        state: present
        login_user: "{{ mysql_root_user }}"
        login_password: "{{ mysql_root_password }}"

    - name: Créer utilisateur iTop et droits
      community.mysql.mysql_user:
        name: "{{ itop_db_user }}"
        password: "{{ itop_db_password }}"
        priv: "{{ itop_db_name }}.*:ALL"
        host: "%"
        state: present
        login_user: "{{ mysql_root_user }}"
        login_password: "{{ mysql_root_password }}"

    - name: Copier le fichier XML de configuration silencieuse pour iTop 3.x
      copy:
        dest: "{{ itop_install_dir }}/setup/itop-config-silent.xml"
        content: |
          <?xml version="1.0" encoding="UTF-8"?>
          <itop_install>
            <target_env>production</target_env>
            <database>
              <server>localhost</server>
              <user>{{ itop_db_user }}</user>
              <pwd>{{ itop_db_password }}</pwd>
              <name>{{ itop_db_name }}</name>
              <prefix>itop_</prefix>
              <db_tls_enabled>false</db_tls_enabled>
              <db_tls_ca></db_tls_ca>
            </database>
            <admin_account>
              <user>admin</user>
              <pwd>admin123</pwd>
              <language>fr</language>
            </admin_account>
            <selected_modules>
              authent-local,itop-config-mgmt,itop-tickets,itop-incident-mgmt,itop-request-mgmt,itop-change-mgmt
            </selected_modules>
          </itop_install>

    - name: Remplacer sort() par un bloc sécurisé dans unattended-install.php
      replace:
        path: "{{ itop_install_dir }}/setup/unattended-install/unattended-install.php"
        regexp: '^\s*sort\(\$aSelectedModules\);'
        replace: |
          if (!is_array($aSelectedModules)) {
              $aSelectedModules = explode(',', $aSelectedModules);
          }
          sort($aSelectedModules);

    - name: Créer dossier datamodels/latest s'il n'existe pas
      file:
        path: "{{ itop_install_dir }}/datamodels/latest"
        state: directory
        owner: www-data
        group: www-data
        mode: '0755'

    - name: Appliquer permissions sur dossier iTop
      file:
        path: "{{ itop_install_dir }}"
        recurse: yes
        owner: www-data
        group: www-data
        mode: '0755'

    - name: Lancer installation silencieuse iTop 3.x via unattended-install.php
      command: >
        php unattended-install.php --param-file=../itop-config-silent.xml
      args:
        chdir: "{{ itop_install_dir }}/setup/unattended-install"
