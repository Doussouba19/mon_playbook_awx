---
- name: Installer iTop 3.x (paquets, téléchargement, Apache)
  hosts: itop
  become: yes

  vars:
    itop_url: "https://sourceforge.net/projects/itop/files/latest/download"
    itop_tmp_zip: "/tmp/itop.zip"
    itop_install_dir: "/var/www/html/web"

  tasks:
    - name: Mettre à jour la liste des paquets
      apt:
        update_cache: yes

    - name: Installer les paquets requis
      apt:
        name:
          - apache2
          - php
          - php-mysql
          - php-xml
          - php-mbstring
          - php-curl
          - php-zip
          - unzip
          - wget
        state: present

    - name: Activer module rewrite d'Apache
      apache2_module:
        name: rewrite
        state: present
      notify: Restart Apache

    - name: Télécharger iTop archive zip dans /tmp
      get_url:
        url: "{{ itop_url }}"
        dest: "{{ itop_tmp_zip }}"
        mode: '0644'

    - name: Supprimer ancien dossier iTop s'il existe
      file:
        path: "{{ itop_install_dir }}"
        state: absent

    - name: Créer dossier iTop
      file:
        path: "{{ itop_install_dir }}"
        state: directory
        owner: www-data
        group: www-data
        mode: '0755'

    - name: Décompresser iTop dans un dossier temporaire
      unarchive:
        src: "{{ itop_tmp_zip }}"
        dest: "/tmp/itop_extract"
        remote_src: yes

    - name: Déplacer contenu décompressé dans le dossier web
      command: mv /tmp/itop_extract/* "{{ itop_install_dir }}"
      args:
        creates: "{{ itop_install_dir }}/index.php"

    - name: Appliquer permissions sur le dossier iTop
      file:
        path: "{{ itop_install_dir }}"
        recurse: yes
        owner: www-data
        group: www-data
        mode: '0755'

  handlers:
    - name: Restart Apache
      service:
        name: apache2
        state: restarted
